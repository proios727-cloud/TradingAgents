# Runtime artifacts
decisions.jsonl
*.jsonl
__pycache__/
*.pyc

# Live-stage runtime state (per-machine; never commit)
.supertrades/

# ...but DO track the forward-validation log (must persist across sessions)
!data/gex_forward_log.jsonl
